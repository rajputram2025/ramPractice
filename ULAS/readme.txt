// winston.add(new winston.transports.MongoDB({
//       db:'mongodb://127.0.0.1:27017/ulas'
// }))

//  uncaughtException
// throw new Error("everything crashed now")

// unhandledRejection

// const p = Promise.reject(new Error("unhandledRejection"));

// p.then(()=> console.log("done"));
